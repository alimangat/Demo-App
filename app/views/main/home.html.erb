<div class="container-fluid">
    <h1>Welcome to Homepage</h1>


    <h3>Categories</h3>
    <% @categories.each do |category| %>
        <%= category.id %>
        <%= category.name %>
        <hr>
    <% end %> 
</div>